name: Deploy to Production

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 45.124.84.70
          port: 22
          username: Administrator
          password: ${{ secrets.SSH_PASSWORD }}
          script: call C:\project\NOBLE-CERT-SYSTEM\deploy.cmd
